# Stage 0 Manual Test Checklist

- Run `npm run dev`.
- Visit `/app/settings` and confirm SettingsLayout renders with two cards.
- Visit `/app` and confirm AppShell sidebar + header render.
- Tab through inputs/buttons on `/app/settings` to confirm focus ring is visible.
