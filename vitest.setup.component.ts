/**
 * Setup file for component tests using jsdom environment.
 * Import this at the top of component test files OR use
 * `// @vitest-environment jsdom` directive.
 */
import "@testing-library/jest-dom/vitest";
